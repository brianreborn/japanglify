<?xml version="1.0" encoding="utf-8"?>
<!--
  No per-target Direct Share targets are published anymore.
  ShareTargetActivity is reached via its normal ACTION_SEND filters as a
  single "Japanglify" entry. It then shows an in-app chooser (current settings
  text/image + saved ShareTargets) — like X-style dropdowns.
  Legacy pinned per-target shortcuts are still honored via EXTRA_SHORTCUT_ID.
-->
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android" />
