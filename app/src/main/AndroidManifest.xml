<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Optional dictionary download only (see DictionaryDownloadManager);
         every lookup after that one-time download is fully offline. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Lets *this* app resolve PROCESS_TEXT targets (diagnostics + in-app menu). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/html" />
        </intent>
    </queries>

    <application
        android:name=".JapanglifyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Japanglify">

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Japanglify"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Share target: the reliable alternative to PROCESS_TEXT requested
          explicitly because PROCESS_TEXT's selection-menu item is entirely
          host-controlled (see ShareTargetActivity's doc comment). Share is
          a first-class OS surface essentially every app supports. The only
          "Japanglify" entry in the system Share sheet: SettingsActivity used
          to also declare ACTION_SEND (opening full Settings with the text
          pre-filled, requiring an extra manual tap to convert), which would
          have shown as a confusing second, differently-behaving entry
          alongside this one, removed in favor of this single, instant,
          always-reliable target.
        -->
        <activity
            android:name=".ShareTargetActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/share_target_label"
            android:launchMode="standard"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Japanglify.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>

        <!--
          PROCESS_TEXT: offered by hosts that query it (Messages, some browsers).
          Not injected into Discord/X custom menus — that is host-side.
          Keep this as close as possible to AOSP / working samples.
        -->
        <activity
            android:name=".ProcessTextActivity"
            android:enabled="true"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/process_text_label"
            android:launchMode="standard"
            android:noHistory="true"
            android:theme="@style/Theme.Japanglify.ProcessText">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/html" />
            </intent-filter>
        </activity>

        <!--
          taskAffinity="" on this and the two activities below: without it,
          each defaults to sharing the app's main task with SettingsActivity.
          Confirmed live via `adb logcat`/ActivityTaskManager: a notification
          or Share action launching one of these attaches to that *same*
          existing task (with SettingsActivity still sitting in its back
          stack from earlier use) instead of getting its own, so finishing
          the brief, transient activity reveals stale Settings instead of
          returning focus to whatever app the user was actually in. An empty
          taskAffinity gives each its own isolated task, so finishing it
          removes that task outright.
        -->
        <activity
            android:name=".clipboard.ProcessClipboardActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Japanglify.Transparent" />

        <activity
            android:name=".clipboard.ClipboardWriteActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Japanglify.Transparent" />

        <service
            android:name=".clipboard.JapanglifyAccessibilityService"
            android:exported="false"
            android:label="@string/a11y_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".clipboard.ClipboardAssistService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Optional clipboard watch when accessibility is not enabled." />
        </service>

        <service
            android:name=".dictionary.DictionaryDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".clipboard.ClipboardAssistReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.japanglify.app.action.STOP_CLIPBOARD_ASSIST" />
                <action android:name="com.japanglify.app.action.COPY_JAPANGLIFY_RESULT" />
                <action android:name="com.japanglify.app.action.COPY_JAPANGLIFY_IMAGE" />
                <action android:name="com.japanglify.app.action.REPLACE_FIELD" />
                <action android:name="com.japanglify.app.action.TRANSLATE" />
                <action android:name="com.japanglify.app.action.TOGGLE_COPY_PAUSE" />
            </intent-filter>
        </receiver>

        <!-- Exposes rendered result images to whatever app performs the paste. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
