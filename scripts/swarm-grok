#!/bin/sh
# Unix trampoline. Windows uses swarm-grok.cmd.
set -e
root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$root"
if command -v python3 >/dev/null 2>&1; then
  exec python3 scripts/swarm-grok.py "$@"
fi
exec python scripts/swarm-grok.py "$@"
